name: C/C++ CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
    - name: make
      run: make -j $(nproc)
    - name: make check
      run: make check -j $(nproc)
